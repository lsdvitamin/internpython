import math
import graphics as gr


def build_house(w, corn, width):
    """ Функция, которая строит дом """
    pass
 

window = gr.GraphWin("Russian game", 500, 500)
build_house(window, gr.Point(100, 100), 100)

print("Ура! Дом построен")
